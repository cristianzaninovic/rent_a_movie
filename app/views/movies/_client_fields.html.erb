<div class="nested-fields">  
  <%= f.input :id, collection: Client.all.map { |field| [field.name, field.id] }, include_blank: false, label: 'Select a client', prompt: "select a client..." %>  
  <%= link_to_remove_association "Remove Client", f %>
</div>