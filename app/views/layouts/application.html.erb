<!DOCTYPE html>
<html>
  <head>
    <title>RentAMovie</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="">
    <header class="site-header">
      <!-- Navbar -->
      <nav class="navbar navbar-expand-lg navbar-dark shadow-5-strong">
        <!-- Container wrapper -->
        <div class="container-fluid">
          <!-- Navbar brand -->          
          <a class="navbar-brand" href="#"><%= image_tag 'logo.png', :class=>"me-2", :height=>"20", :alt=>"Logo", :loading=>"lazy"%>
            <small>Rent a Movie</small>
          </a>

          <!-- Toggle button -->
          <button
            class="navbar-toggler"
            type="button"
            data-mdb-toggle="collapse"
            data-mdb-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <i class="fas fa-bars"></i>
          </button>

          <!-- Collapsible wrapper -->
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <!-- Left links -->
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0" >
              <li class="nav-item">
                <%= link_to "Home", root_path, :class=>"nav-link active", "aria-current"=>"page" %>
              </li>
              <li class="nav-item">
                <%= link_to "Movies", movies_path, :class=>"nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Clients", clients_path, :class=>"nav-link" %>
              </li>
              
            </ul>
            <!-- Left links -->
          </div>
          <!-- Collapsible wrapper -->
        </div>
        <!-- Container wrapper -->
      </nav>
      <!-- Navbar -->      
    </header>
    <div class="container float-start rounded bg-white p-3 ms-5">
      <div class="row">
        <div class="col">
          <%= yield %>
        </div>        
      </div>
    </div>  
  </body>
</html>
